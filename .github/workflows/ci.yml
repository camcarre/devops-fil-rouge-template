name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lint-test:
    name: Lint + Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Lint (ruff)
        run: ruff check src tests

      - name: Audit dependencies (pip-audit)
        run: python -m pip_audit --progress=off

      - name: Tests (pytest)
        env:
          DATABASE_URL: sqlite://
          SECRET_KEY: ci-test-secret
        run: pytest -q

  security-scan:
    name: Security scan (Trivy)
    runs-on: ubuntu-latest
    needs: lint-test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build API image
        run: docker build -t devops-fil-rouge-template-api:latest .

      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin v0.44.0

      - name: Scan API image
        run: trivy image --exit-code 1 --severity HIGH,CRITICAL devops-fil-rouge-template-api:latest

  integration:
    name: Build + smoke compose (secrets)
    runs-on: ubuntu-latest
    needs: lint-test
    steps:
      - uses: actions/checkout@v4

      - name: Générer .env depuis les GitHub Secrets
        env:
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
        run: |
          {
            echo "POSTGRES_USER=$POSTGRES_USER"
            echo "POSTGRES_PASSWORD=$POSTGRES_PASSWORD"
            echo "POSTGRES_DB=$POSTGRES_DB"
            echo "SECRET_KEY=$SECRET_KEY"
            echo "PORT=8000"
          } > .env

      - name: Démarrer la stack (build inclus)
        run: docker compose up -d --build

      - name: Attendre que l'API réponde sur /health
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/health; then echo " OK"; exit 0; fi
            sleep 3
          done
          echo "API jamais healthy — logs :"
          docker compose logs
          exit 1

      - name: Teardown
        if: always()
        run: docker compose down -v
